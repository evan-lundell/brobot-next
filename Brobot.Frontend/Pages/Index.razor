@page "/"
@inject JwtAuthenticationStateProvider JwtAuthenticationStateProvider

<PageTitle>Brobot</PageTitle>

<AuthorizeView>
    <Authorized>
        <text>Placeholder for dashboard?</text>
    </Authorized>
    <NotAuthorized>
        @if (JwtAuthenticationStateProvider.IsLoggedIn)
        {
            <SyncDiscordButton />
        }
        else
        {
            <LoginForm />
        }
    </NotAuthorized>
</AuthorizeView>
